// The real options live in lib/firebase_options.dart — that is where
// `flutterfire configure` writes by default (project manger-app-3aa3b).
// Re-exported here so AppFirebase's import keeps working, and so re-running
// flutterfire later updates the app with no extra step.
export 'package:manger_plus/firebase_options.dart';
